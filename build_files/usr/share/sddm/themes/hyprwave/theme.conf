[General]
# Background is copied next to this file at build time (see build.sh) from the
# shared default wallpaper, so the login screen and desktop match.
background=background.png
fontFamily=JetBrains Mono
# Palette (matches ghostty / waybar / walker / hyprlock)
bgColor=#15052e
fgColor=#e0e0ff
pink=#ff2d95
cyan=#00f0ff
purple=#b967ff
